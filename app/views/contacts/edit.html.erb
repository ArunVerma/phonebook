<% content_for(:page_header) do %>
  Edit Contact
<% end %>
<%= form_for @contact, url: contact_path(@contact.id), method: 'patch' do |f| %>
  <%= render partial: 'contacts/form', locals: {f: f} %>
<% end %>
